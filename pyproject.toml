[project]
name = "kyykkacom"
version = "0.1.0"
description = "Website for kyykka.com"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "certifi==2019.11.28",
    "chardet==3.0.4",
    "coreapi==2.3.3",
    "coreschema==0.0.4",
    "Django==5.1.7",
    "django-cors-headers==4.7.0",
    "djangorestframework==3.16.0",
    "gunicorn==20.0.4",
    "idna==2.8",
    "itypes==1.1.0",
    "Jinja2==3.1.6",
    "MarkupSafe==3.0.3",
    "PyMySQL==1.0.3",
    "openapi-codec==1.3.2",
    "pymemcache==4.0.0",
    "python-dateutil==2.9.0.post0",
    "python-memcached==1.59",
    "pytz==2025.2",
    "simplejson==3.17.0",
    "six==1.13.0",
    "sqlparse==0.5.3",
    "text-unidecode==1.2",
    "uritemplate==3.0.0",
    "urllib3==1.26.1",
    "tqdm",
]

[project.optional-dependencies]
dev = [
    "django-silk==5.3.2",
    "django-debug-toolbar==5.1.0",
    "Faker==1.0.4",
    "drf-yasg==1.21.10",
    "django-rest-swagger==2.2.0",
    "pyright==1.1.397",
]

# Optional group for native MySQL driver. Install with `pip install -e .[mysql]` if you
# want to use `mysqlclient` instead of the default PyMySQL (requires system dev libs).
mysql = ["mysqlclient==2.2.6"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["kyykka", "api", "nkl"]
